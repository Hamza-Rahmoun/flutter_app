import 'package:flutter/material.dart';

class PageZero extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('page zero'),
      ),
    );
  }
}
